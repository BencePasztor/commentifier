import { Router } from "express";
import asyncWrapper from "@/utils/asyncWrapper";

const router = Router()

//TODO:
// router.get('/me', () => {}) //Get the current user
// router.patch('/profile', () => {}) //avatar
// router.patch('/password-change', () => {}) //password

export default router